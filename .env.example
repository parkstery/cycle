# 표고 API 프록시 (선택). 앱 번들이 https://localhost 등으로만 서빙될 때 Vercel 등 실제 배포 URL을 지정합니다.
# VITE_ELEVATION_PROXY_ORIGIN=https://your-app.vercel.app

# Mapbox GL (Phase 2 베이스맵). default public token — 이 파일을 복사해 .env.local 로 쓰고 값만 채우면 됩니다.
# VITE_MAPBOX_ACCESS_TOKEN=
