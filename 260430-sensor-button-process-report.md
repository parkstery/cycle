# 260430-센서 버튼 처리 과정 보고서

## 목적
- HUD 우측 속도 영역에 센서 빠른 제어 버튼을 추가하고, 센서 연결 상태를 직관적으로 표현해 사용자의 혼동을 줄인다.
- 라우트 패널의 `Sensor` 버튼(상세 설정 진입)과 HUD 버튼(빠른 On/Off)을 역할 분리한다.

## 적용 범위
- 대상 파일: `App.tsx`
- 대상 기능:
  - HUD 블루투스 센서 버튼 UI/UX
  - 센서 On/Off 토글 동작
  - 센서 스캔/연결 중 상태 표현
  - Feel 바 노출 조건 및 HUD 텍스트 레이아웃 안정화

## 구현 타임라인 (핵심 커밋)
- `be9a961`: HUD 블루투스 버튼 추가, 센서 스캔/감지 간편 토글 1차 구현
- `d58d431`: 자동 재연결 비활성 상태에서도 간편 켜기 시 1회 스캔/연결 시도
- `2faf2e9`: 버튼 배경 제거, 스캔 중 점멸 처리 보강
- `9589a36`: Off 상태에서도 버튼 상시 표시, On/Off/Busy 색 규칙 정리
- `3e4702a`: 버튼 크기 60% 축소, 검은 글로우 추가, 속도열 LED 원 제거
- `8e071ac`: On이지만 미연결(유휴) 상태에서는 버튼 흰색 처리
- `d9ee236`: 아이콘+`ON/OFF` 텍스트를 한 버튼으로 통합, 색/동작 동기화
- `461a8ee`: 아이콘에도 ON/OFF 텍스트와 동일한 검정 글로우 적용
- `ba4bd56`: 버튼 외곽 그린 라운드 테두리 + 4px 좌측 이동
- `301b6a2`: Feel 바 숨김 시에도 공간 유지해 HUD 텍스트 상하 흔들림 제거
- `ecee89b`: Feel 바 표시 조건을 “센서 모드 On”에서 “실제 센서 연결됨”으로 변경

## 최종 동작 사양

### 1) HUD 센서 버튼 동작
- 버튼 클릭 시 토글:
  - Off -> On: 센서 스캔/연결 시도(자동재연결 설정 여부에 따라 persistent reconnect 정책 반영)
  - On -> Off: 스캔 중지, persistent reconnect 중단, 연결 해제
- 라우트 패널 `Sensor` 버튼은 상세 메뉴 진입 전용으로 유지.

### 2) 버튼 시각 규칙
- Off: 흰색
- On + 스캔/커넥팅 중: 녹색 점멸
- On + 연결됨: 녹색 고정
- On + 미연결(유휴): 흰색

### 3) ON/OFF 레이블
- 버튼 우측 텍스트를 같은 버튼 안에 통합:
  - 연결됨: `ON`
  - 미연결: `OFF`
- 아이콘과 텍스트는 동일 색/점멸 상태를 공유.

### 4) Feel 바 노출 정책
- 표시 조건: `sensorDriveEnabled && sensorHubConnected`
- 즉, 연결 모드로 진입했더라도 실제 연결 전/실패 상태에서는 Feel 바 미표시.
- 단, HUD 텍스트(속도/케이던스/거리) 위치 안정성을 위해 Feel 바 영역 높이는 항상 유지(invisible 방식).

## UX 개선 효과
- 센서 상태와 조작 가능 상태가 일관되게 표현되어 사용자가 현재 상태를 즉시 이해할 수 있다.
- 연결 실패 상황에서 Feel 바가 노출되지 않아 잘못된 기대를 줄인다.
- HUD 텍스트가 상태 변화마다 위아래로 움직이지 않아 시각 피로와 오동작 인지를 줄인다.

## 비고
- 본 변경은 UI 상태 표현과 센서 제어 플로우 정합성 개선이 목적이며, 라우트/주행 핵심 계산 로직은 변경하지 않았다.
