---
description: 코드 수정 후 Git 커밋·푸시 절차
alwaysApply: true
---

# 코드 수정 후 Git 처리

- **코드 수정을 마친 뒤** 반드시 다음을 수행한다.
  1. 프로젝트 루트에서 `npm run build` 실행 (Vite 프로덕션 빌드)
  2. `npx cap sync` 실행 (웹 자산을 Android 등 네이티브에 반영)
  3. `git add`로 변경된 파일 스테이징
  4. 의미 있는 커밋 메시지와 함께 `git commit` 실행
  5. `git push`까지 실행
- 커밋 메시지는 변경 내용을 한글이나 영어로 요약해 작성한다. (예: `fix: 거리뷰 갭 이후 재개, 맵 스케일바 표시`)
